# Copy to .env (gitignored — NEVER commit real credentials) and fill in.
DRUPAL_BASE_URL=https://tercdev.sf.ucdavis.edu
DRUPAL_USER=your-jsonapi-user
DRUPAL_PASS=your-password
# Optional: extra header for WAF/CDN bypass rules (see README). Use the
# real header name configured in the Cloudflare rule — do not commit it.
#SYNC_HEADER=X-Your-Header-Name: shared-secret-here
